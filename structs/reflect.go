package main

func main() {
	/*
		REFLECT
		recflect.TypeOf  => main.order
		recflect.ValueOf  => {456 6}
		recflect.TypeOf.Kind()  => Struct

		func createQuerry (q any) {
			v := reflect.valueOf(q)
			v.numField() = 2 (nb de donnée dans valueOf)
		}

		for i=0 ; i < v.numField() ; i++ {
			fmt.printF("type: %T value:%v", v.field(i), v.field(i))
								!!  type:reflect.value value: 456 6
		}
	*/
}
